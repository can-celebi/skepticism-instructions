// app.js — bootstrap the left-column prototype.
window.addEventListener('DOMContentLoaded', () => {
  // ?reset kept for parity with the other prototypes (no persistence yet here)
  App.stage.mount();
});
